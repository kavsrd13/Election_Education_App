steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/promptwatvirtual/election-journey-repo/election-journey:latest'
      - '.'

  # Step 2: Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/promptwatvirtual/election-journey-repo/election-journey:latest'

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'election-journey'
      - '--image=us-central1-docker.pkg.dev/promptwatvirtual/election-journey-repo/election-journey:latest'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=3'
      - '--set-secrets=GEMINI_API_KEY=gemini-api-key:latest'

images:
  - 'us-central1-docker.pkg.dev/promptwatvirtual/election-journey-repo/election-journey:latest'

options:
  logging: CLOUD_LOGGING_ONLY
